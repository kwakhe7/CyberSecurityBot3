<UserControl x:Class="ChatbotPart2.TaskView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:ChatbotPart2"
             MinWidth="520" MinHeight="360">
    <UserControl.Resources>
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#1E88E5"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="#E53935"/>
        </Style>

        <Style TargetType="ListViewItem" x:Key="TaskItemStyle">
            <Setter Property="Padding" Value="8"/>
            <Setter Property="Margin" Value="4,2"/>
            <Setter Property="Background" Value="#1F1F1F"/>
            <Setter Property="BorderBrush" Value="#2E2E2E"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListViewItem">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#263238"/>
                                <Setter Property="BorderBrush" Value="#90CAF9"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="#455A64"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="12" Background="Transparent">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="1.1*" />
        </Grid.ColumnDefinitions>

        <!-- Left: Task list -->
        <Border Grid.Column="0" Background="#20232A" Padding="12" CornerRadius="8" Margin="0,0,12,0">
            <DockPanel>
                <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" HorizontalAlignment="Stretch">
                    <TextBlock Text="Tasks" FontSize="16" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" DockPanel.Dock="Right" Margin="8,0,0,0">
                        <Button Content="Refresh" Command="{Binding RefreshCommand}" Style="{StaticResource PrimaryButton}" Width="80"/>
                    </StackPanel>
                </StackPanel>

                <ListView ItemsSource="{Binding Tasks}" SelectedItem="{Binding SelectedTask, Mode=TwoWay}" Margin="0,12,0,0"
                          ItemContainerStyle="{StaticResource TaskItemStyle}">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="180" />
                                    <ColumnDefinition Width="120" />
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="{Binding Title}" FontWeight="SemiBold" Foreground="White" FontSize="13" TextWrapping="Wrap"/>
                                    <TextBlock Text="{Binding Description}" Foreground="#BDBDBD" FontSize="12" TextWrapping="Wrap" Margin="0,4,0,0"/>
                                </StackPanel>

                                <TextBlock Grid.Column="1" Text="{Binding ReminderUtc, Converter={StaticResource UtcToLocalConverter}}" Foreground="#90CAF9" VerticalAlignment="Center" />

                                <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right">
                                    <TextBlock Text="{Binding IsCompleted, Converter={StaticResource BoolToStatusConverter}}" VerticalAlignment="Center" Foreground="#CFCFCF" Margin="0,0,8,0"/>
                                    <Button Content="Complete" Command="{Binding DataContext.CompleteCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                            CommandParameter="{Binding}" Style="{StaticResource PrimaryButton}" Width="80" Margin="0,0,4,0"
                                            IsEnabled="{Binding IsCompleted, Converter={StaticResource InverseBoolConverter}}"/>
                                    <Button Content="Delete" Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}" Style="{StaticResource DangerButton}" Width="70"/>
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </DockPanel>
        </Border>

        <!-- Right: Create new task form -->
        <Border Grid.Column="1" Background="#121417" Padding="12" CornerRadius="8">
            <StackPanel>
                <TextBlock Text="New Task" FontSize="16" FontWeight="Bold" Foreground="White" Margin="0,0,0,8"/>
                <TextBlock Text="Title" Foreground="#BDBDBD" FontSize="12"/>
                <TextBox Text="{Binding NewTitle, UpdateSourceTrigger=PropertyChanged}" Background="#1F1F1F" Foreground="White" Margin="0,4,0,8"/>

                <TextBlock Text="Description" Foreground="#BDBDBD" FontSize="12"/>
                <TextBox Text="{Binding NewDescription, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" Height="100" TextWrapping="Wrap" Background="#1F1F1F" Foreground="White" Margin="0,4,0,8"/>

                <TextBlock Text="Reminder (optional)" Foreground="#BDBDBD" FontSize="12"/>
                <DatePicker SelectedDate="{Binding NewReminderLocal}" Background="#1F1F1F" Foreground="White" Margin="0,4,0,8"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
                    <Button Content="Add Task" Command="{Binding AddCommand}" Style="{StaticResource PrimaryButton}" Width="100"/>
                </StackPanel>

                <TextBlock Text="Tip: use reminders to get notified. Completed tasks move to the bottom." Foreground="#9E9E9E" FontStyle="Italic" Margin="0,12,0,0" TextWrapping="Wrap"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>