<Application x:Class="ChatbotPart2.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:ChatbotPart2"
             Startup="Application_Startup">
    <Application.Resources>
        <ResourceDictionary>
            <!-- Converters used across the app -->
            <local:UtcToLocalConverter x:Key="UtcToLocalConverter" />
            <local:BoolToStatusConverter x:Key="BoolToStatusConverter" />
            <local:InverseBoolConverter x:Key="InverseBoolConverter" />

            <ResourceDictionary.MergedDictionaries>
                <!-- App logo resources (vector) -->
                <ResourceDictionary Source="Assets/Logo.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Application.Resources>
</Application>
